# Scraper and API Server for McDonald's Products

This project contains a Scrapy-based web scraper and a FastAPI server.

## How to Run

```bash
poetry install
cd macdonalds
poetry run python run.py
```
When the script is completed, the server will be available by 127.0.0.1:8000
```bash
127.0.0.1:8000/docs
```